tasks:
  branch: "agent/jcode-6gp-disk-safety"
  created: "2026-08-23"
  spec_path: "specs/016-worktree-disk-safety/spec.yaml"
  plan_path: "specs/016-worktree-disk-safety/plan.yaml"

summary:
  total_tasks: 9
  total_phases: 4
  parallel_opportunities: 0
  estimated_complexity: "medium"

phases:
  - number: 1
    title: "Specification and characterization"
    purpose: "Freeze the destructive-safety contract and reproduce preserved draft gaps."
    tasks:
      - id: "T001"
        status: "Completed"
        title: "Validate the disk-safety specification, plan, and acceptance matrix"
        type: "setup"
        parallel: false
        story_id: null
        file_path: "specs/016-worktree-disk-safety/"
        dependencies: []
        acceptance_criteria:
          - "Autospec artifact validation passes for spec.yaml, plan.yaml, and tasks.yaml."
      - id: "T002"
        status: "Completed"
        title: "Add focused failing tests for threshold, configuration, containment, exclusions, dry-run, and apply"
        type: "test"
        parallel: false
        story_id: null
        file_path: "scripts/test_disk_safety.py"
        dependencies: ["T001"]
        acceptance_criteria:
          - "The preserved draft fails deletion-time revalidation, nested-recency, and live-session uncertainty tests before repair."
  - number: 2
    title: "Report and preflight"
    purpose: "Expose bounded disk visibility and stop broad Cargo work below the reserve."
    tasks:
      - id: "T003"
        status: "Completed"
        title: "Implement bounded worktree target reporting and inclusive configurable reserve checking"
        type: "implementation"
        parallel: false
        story_id: "US-001"
        file_path: "scripts/disk_safety.py"
        dependencies: ["T002"]
        acceptance_criteria:
          - "Report and check commands expose filesystem headroom, bounded target usage, and inclusive CLI/env/default threshold behavior."
      - id: "T004"
        status: "Completed"
        title: "Insert the disk reserve preflight before every Cargo command in broad guardrails"
        type: "implementation"
        parallel: false
        story_id: "US-001"
        file_path: "scripts/check_guardrails.sh"
        dependencies: ["T003"]
        acceptance_criteria:
          - "Low space exits before the first Cargo command with actionable guidance; adequate reserve preserves guardrail ordering."
  - number: 3
    title: "Fail-closed cleanup"
    purpose: "Select and apply only reviewable generated-artifact cleanup."
    tasks:
      - id: "T005"
        status: "Completed"
        title: "Implement dry-run selection for registered contained non-main inactive clean old targets"
        type: "implementation"
        parallel: false
        story_id: "US-002"
        file_path: "scripts/disk_safety.py"
        dependencies: ["T003"]
        acceptance_criteria:
          - "Default cleanup reports eligible target directories and deletes nothing."
      - id: "T006"
        status: "Completed"
        title: "Implement explicit apply with deletion-time registration, activity, dirty-state, containment, and newest-artifact revalidation"
        type: "implementation"
        parallel: false
        story_id: "US-002"
        file_path: "scripts/disk_safety.py"
        dependencies: ["T005"]
        acceptance_criteria:
          - "Temporary-fixture apply removes only a still-eligible target and refuses uncertain or changed state."
  - number: 4
    title: "Documentation and delivery evidence"
    purpose: "Close operator guidance, focused validation, review, and merge handoff."
    tasks:
      - id: "T007"
        status: "Completed"
        title: "Document commands, precedence, exclusions, rebuild expectations, and fail-closed behavior"
        type: "documentation"
        parallel: false
        story_id: null
        file_path: "docs/DISK_SAFETY.md"
        dependencies: ["T004", "T006"]
        acceptance_criteria:
          - "Developer docs explain report, check, dry-run, apply, configuration precedence, exclusions, uncertainty, and Cargo rebuild expectations."
      - id: "T008"
        status: "Completed"
        title: "Run focused tests, non-destructive real command smoke, independent safety review, formatting, and eligible guardrails"
        type: "test"
        parallel: false
        story_id: null
        file_path: "specs/016-worktree-disk-safety/validation.md"
        dependencies: ["T007"]
        acceptance_criteria:
          - "Focused tests pass first, real report/check/dry-run succeed, independent deletion review has no blockers, and broad guardrails run only after disk-check passes."
      - id: "T009"
        status: "Completed"
        title: "Commit the reviewed change and prepare the risk-gated merge handoff"
        type: "setup"
        parallel: false
        story_id: null
        file_path: "specs/016-worktree-disk-safety/validation.md"
        dependencies: ["T008"]
        acceptance_criteria:
          - "Owned files are explicitly staged and committed, the Bead records validation and risk, and medium/high-risk landing waits for user approval."
